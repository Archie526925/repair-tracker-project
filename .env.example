# Docker Compose environment defaults
# Copy to .env and modify before running `docker compose up -d`

# PostgreSQL password (used by both postgres container and DATABASE_URL)
POSTGRES_PASSWORD=changeme

# JWT secret for API authentication (REQUIRED - change this!)
JWT_SECRET=change-me-to-a-random-secret
